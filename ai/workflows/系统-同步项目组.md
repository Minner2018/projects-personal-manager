# 系统-同步项目组

目标：根据 `registry.yaml` 更新 `programs/*/_program.yaml` 的 `projects` 字段。

步骤：

1. 读取 `registry.yaml`。
2. 读取每个 `_program.yaml` 的人工字段。
3. 根据 `members` 生成 `projects`。
4. 只保留组内关系。
5. 只写回 `projects` 字段。
