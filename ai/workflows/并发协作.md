# 并发协作

目标：降低多个 AI 同时工作时的文件覆盖风险。

步骤：

1. 运行 `bun run check:concurrency` 检查现有并发状态。
2. 发现文件已上锁时，停止修改并报告冲突。
3. 修改前用 `bun run lock:create -- --file <path> --owner <agent_id> --reason <reason>` 创建锁。
4. 完成、暂停或受阻时更新交接并释放自己的锁。
