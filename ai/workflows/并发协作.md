# 并发协作

目标：降低多个 AI 同时工作时的文件覆盖风险。

步骤：

1. 运行 `bun run check:concurrency` 检查现有并发状态。
2. 在任务记录中声明计划修改文件。
3. 修改前用 `bun run lock:create -- --file <path> --task <task> --owner <owner> --reason <reason>` 为高冲突风险文件创建锁。
4. 发现文件被其他任务声明或锁定时，停止修改并报告冲突。
5. 完成、暂停或受阻时更新交接并释放自己的锁。
