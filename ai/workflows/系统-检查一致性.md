# 系统-检查一致性

目标：检查 `.project.yaml`、`registry.yaml`、`_program.yaml` 和 AI 项目摘要之间的不一致。

步骤：

1. 校验项目事实源。
2. 校验 registry 是否由事实源生成。
3. 校验项目组是否由 registry 和 members 生成。
4. 校验 AI 摘要与事实源是否明显冲突。
5. 输出 `error` 和 `warning`。
