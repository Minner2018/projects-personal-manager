# 实现注释编写标准

## 目录

1. [目标](#目标)
2. [适用范围](#适用范围)
3. [“是什么”：代码对象与方法](#是什么代码对象与方法)
4. [“怎么做”：方法内部流程](#怎么做方法内部流程)
5. [“为什么”：业务与流程决策](#为什么业务与流程决策)
6. [注释质量要求](#注释质量要求)
7. [语言适配](#语言适配)
8. [审核与修正清单](#审核与修正清单)

## 目标

让实现具备注释主导的分层可理解性：阅读者仅阅读注释、代码结构和少量高价值代码，就应能够理解代码是什么、如何工作以及为什么这样设计，而无需逐行阅读普通实现语句或依赖原始对话和作者记忆。

将注释作为实现的主要阅读入口，将类与方法签名、核心调用、关键条件、重要数据转换、状态变化和最终返回作为注释的事实验证。

所有新增或修改的注释使用中文；项目明确规定其他语言时遵循项目规则。

## 适用范围

将本标准应用于 `[I]` 任务中新增或修改的所有人工编写源文件，包括生产代码和测试代码。对每个变更源文件，审核并覆盖文件内全部人工编写的代码对象、方法、函数和构造方法，而不只覆盖本次直接修改的方法或行。

不要为了满足本标准修改第三方、vendor、压缩、自动生成或本次任务未触及的代码。若必须直接修改生成代码，先确认其事实源和再生成策略，并在结果中说明风险。

## “是什么”：代码对象与方法

### 代码对象

为每个变更源文件中的类、接口、枚举、记录类型、模块级对象及语言中的相近层级代码对象添加块级文档注释。说明：

- 它在系统或业务中的职责；
- 它代表、管理或协调的对象；
- 必要时说明它与主要协作者的边界。

不要只把名称拆成自然语言，也不要在对象注释中复述所有字段。

### 方法与可执行单元

为每个变更源文件中的方法、函数和构造方法添加块级文档注释。对包含实质逻辑的匿名函数、回调或相近可执行单元，在定义前说明其职责。

方法注释至少说明方法是什么以及完成什么职责。存在前置条件、重要副作用、异常语义或调用顺序约束时一并说明。

参数和返回说明遵循以下要求：

- 方法包含两个或更多参数时，使用语言对应的 `@param`、`:param` 或 XML 参数标签逐项说明全部参数；
- 单参数的含义、单位、范围、来源或可空性不明显时，也说明该参数；
- 返回值不是 `void`、`None` 或等价空返回，且属于对象、集合、结果包装或其他非基础类型时，使用语言对应的 `@return`、`:return` 或 XML 返回标签说明结果内容和语义；
- 标签说明业务含义和约束，不只重复参数名或类型名。

## “怎么做”：方法内部流程

### 逻辑阶段

在方法内部按逻辑阶段添加行级注释，使这些注释按顺序阅读时能够串联出方法的主要执行流程。

常见阶段包括输入校验、数据准备、查询、过滤、转换、核心计算、持久化、事件发布、结果组装和资源清理。阶段以实际职责划分，不按固定行数机械切块。

满足下列任一情况时，通常应开始一个带注释的逻辑阶段：

- 一组语句共同完成一个可命名的子目标；
- 数据从一种业务状态或表示转换到另一种；
- 执行从准备阶段进入核心处理、持久化或输出阶段；
- 较长方法中的下一段代码需要重新建立阅读上下文。

### 复杂调用

在复杂方法或重要组件调用行前添加行级注释，说明该调用在当前流程中怎么做以及承担什么阶段职责。

以下调用通常视为复杂或重要调用：

- 跨模块、跨层、远程服务、仓储、消息、事务或外部系统调用；
- 算法、批量处理、坐标或格式转换、序列化、解析和聚合调用；
- 涉及并发、异步、安全、缓存、重试、补偿或资源生命周期的调用；
- 方法名无法让不了解上下文的阅读者直接判断其业务作用的调用。

不要为简单 getter、普通集合访问、显然的工具方法或已被紧邻阶段注释充分覆盖的调用重复添加低价值注释。

## “为什么”：业务与流程决策

在由业务规则、流程状态或非显然技术约束产生的判断和分支前添加行级注释，说明分支为什么存在以及它保护什么行为。

重点覆盖：

- 业务准入、排除、优先级和状态转换规则；
- 提前返回、跳过、降级、兜底、补偿和重试条件；
- `if`、`switch`、模式匹配、异常分支或循环终止中承载的流程规则；
- 兼容性、性能、并发、安全、历史数据或外部系统限制；
- 看似可以删除、合并或改序，但实际受约束的逻辑。

解释规则来源或设计原因，不要只把条件表达式翻译成中文。

低价值示例：

```java
// 判断任务是否结束
if (task.isFinished()) {
```

高价值示例：

```java
// 业务规定已结束的飞行任务不能再次参与实时轨迹融合
if (task.isFinished()) {
```

普通语法控制、显然的空值防御或没有额外业务与流程含义的简单分支，不要求机械添加“进入循环”“判断是否为空”之类的注释；但其目的或影响需要推理时必须说明。

## 注释质量要求

使注释满足以下要求：

- **准确**：与最终代码、边界和顺序一致；修改逻辑时同步修改注释；
- **就近**：放在对应对象、阶段、复杂调用或决策之前；
- **连续**：方法内阶段注释能够形成完整且顺序正确的流程骨架；
- **具体**：使用业务和系统概念，避免“处理数据”“调用方法”等空泛表述；
- **解释性**：补充代码无法直接表达的职责、流程和原因，不逐行复述语法；
- **适量**：覆盖所有需要理解的部分，同时避免重复注释同一事实；
- **可维护**：不记录临时讨论、过期计划、作者心理过程或容易与代码失真的细节。

优先通过清晰命名和合理拆分降低理解成本，再用注释保存无法仅靠代码表达的结构、流程和决策语义。不要用长注释掩盖职责混乱的方法。

## 语言适配

使用目标语言和项目既有风格支持的文档注释形式：

- Java、Kotlin：Javadoc/KDoc；
- TypeScript、JavaScript：TSDoc/JSDoc；
- Python：Docstring；
- C#：XML documentation comments；
- Go：紧邻声明、以对象名称开头的文档注释；
- 其他语言：使用其标准文档注释与普通行注释形式。

语言不支持 `@param` 或 `@return` 时，使用该语言等价的结构化参数和返回说明。不要为了标签形式破坏目标语言的标准风格。

## 审核与修正清单

实现完成后逐项审核所有变更源文件，并在验证前修正发现的问题：

1. 每个变更源文件中的所有类及相近层级代码对象是否都有说明“是什么”的块级注释？
2. 每个变更源文件中的所有方法、函数和构造方法是否都有块级注释？
3. 多参数方法是否逐项说明全部参数？必要的单参数说明是否完整？
4. 非空、非基础类型返回是否有结构化返回说明？
5. 方法内部的阶段注释能否按顺序串联出“怎么做”？
6. 每个复杂或重要调用前是否说明其当前流程职责？
7. 业务、流程和非显然技术分支是否解释了“为什么”？
8. 是否存在只复述名称、类型、条件或语法的低价值注释？
9. 注释是否与最终代码一致，没有因实现调整而过期？
10. 隐去普通实现语句，只保留注释、结构和少量高价值代码后，阅读者是否仍能正确复述代码是什么、如何工作以及为什么这样设计？
